/**
 * 加法函数
 * @param x - 被加数
 * @param y - 加数
 * @returns 两数之和
 * @example
 * ```typescript
 * add(1, 2) // 3
 * ```
 *
 * @public
 */
export function add(x: number, y: number): number {
  return x + y
}
